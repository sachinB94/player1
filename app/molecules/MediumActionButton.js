import React from 'react';

import { ActionButton } from '../atoms';

const MediumActionButton = props => (
  <ActionButton iconStyle={{ width: 80, height: 80 }} {...props} />
);

export default MediumActionButton;
